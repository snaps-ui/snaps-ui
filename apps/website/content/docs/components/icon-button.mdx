---
title: IconButton
description: A button that displays an icon instead of text, ideal for compact actions.
---

<ExampleTabs name="icon-button-basics" scope="icon-button" />

## Usage

```tsx
import { IconButton } from '@snaps-ui/react'

;<IconButton>
  <YourCustomIcon />
</IconButton>
```

It accepts all props available in **Button** component.

## Sizes

You can control the size of the Button using the **size** prop.

<ExampleTabs name="icon-button-size" scope="icon-button" />

## Variants

You can control the appearance of the icon-button using the **variant** prop.

<ExampleTabs name="icon-button-variant" scope="icon-button" />
