---
title: Button
description: Used to trigger an action or event
---

<ExampleTabs name="button-basics" scope="button" />

## Usage

```tsx
import { Button } from '@snaps-ui/react/button'
;<Button>Click Me</Button>
```

## Sizes

You can control the size of the Button using the **size** prop.

<ExampleTabs name="button-size" scope="button" />

## Variants

You can control the appearance of the button using the **variant** prop.

<ExampleTabs name="button-variant" scope="button" />

## Renders a button as link

Use the **asChild** prop to render the button as another element, such as a link.

<ExampleTabs name="button-as-link" scope="button" />

## Ripple Effect

Use the **ripple** prop to enable a ripple animation.

By default, Snaps UI uses the current button color as the ripple background.

<ExampleTabs name="button-ripple" scope="button" />

## Loading Button

Use the **`isLoading`** prop to show a loading spinner inside the button while an action is in progress.

<ExampleTabs name="button-loading" scope="button" />

## Loading Button with Text

Use the **`isLoading`** and **`loadingText`** props together to display custom text while loading.

<ExampleTabs name="button-loading-text" scope="button" />

## Disabled

Use the **disabled** prop to disable the button.

<ExampleTabs name="button-disabled" scope="button" />

## Spinner Placement

Use the **spinnerPlacement** prop to set the placement of the spinner.

<ExampleTabs name="button-spinner-placement" scope="button" />

## With Custom Spinner

Use the **spinner** prop to change the spinner.

<ExampleTabs name="button-custom-spinner" scope="button" />
